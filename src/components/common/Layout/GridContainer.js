import React from "react";
import {jsx, css} from "@emotion/react"

export default function GridContainer({rows}){
    return(
        <div css={css({
            display:'grid',

        })}>

        </div>
    )
}